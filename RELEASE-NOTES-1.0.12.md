# NetPulse Monitor 1.0.12

- Adds confirmed TP-Link router restart controls to the desktop and Companion apps.
- Shows `Scan bands & cells` correctly without a duplicated ampersand.
- Keeps the current time-of-day LTE History group first and expanded.
- Collapses non-current groups automatically and cycles the expanded group when the configured location enters a new time period.
